<?xml version="1.0"?>
<ruleset name="Studio15">
    <description>Studio 15 coding standard.</description>

    <arg name="colors"/>
    <arg name="extensions" value="php"/>

    <file>../src/</file>

    <exclude-pattern>Kernel.php</exclude-pattern>

    <!-- Rule: There is no "else" -->
    <rule ref="phpcs.ControlStructures.NoElse"/>
</ruleset>
